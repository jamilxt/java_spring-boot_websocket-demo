# java_spring-boot_websocket-demo
With or Without SockJs configured

## Commands for gradle/Spring
- `gradlew clean`: clean the generated build dir
- `gradlew bootRun`: download the dependencies & run the application
